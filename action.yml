# action.yml
name: 'Yarn Test'
description: 'Run yarn test'
outputs:
  time:
    description: 'The time'
runs:
  using: 'docker'
  image: 'Dockerfile'
branding:
  icon: 'check-square'  
  color: 'orange'
